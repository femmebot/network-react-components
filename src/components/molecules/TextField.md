```
<Flex justifyContent="space-between">
  <TextField onChange={() => {}} />
  <TextField placeholder="Placeholder" onChange={() => {}} />
  <TextField label="Some label" onChange={() => {}} />
  <TextField onChange={() => {}} error="Some error"/>
</Flex>
<TextField width={1} onChange={() => {}} placeholder="Full Width"/>
```
